name: Cypress Tests

on: push

jobs:
  cypress-test:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - uses: cypress-io/github-action@v5
        with:
          browser: chrome
      - run: "start-server-and-test dev http://localhost:3000 \"cypress run --e2e\""
